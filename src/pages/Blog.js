import Aboutusbanner from "../components/AboutUsBanner"
import BlogPost from "../components/BlogPost"

const Blog = () => {
    return (
        <div>
            <Aboutusbanner titleText="BLOG" infoText="Capitalise on low hanging fruit to identify a ballpark value added activity to beta performance test. Override the digital divide."/>
            <BlogPost />
       </div>
    );
}

export default Blog;